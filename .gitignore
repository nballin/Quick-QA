# Local clone of TRM (separate repo); use: git clone https://github.com/nballin/TinyRecursiveModels.git
TinyRecursiveModels/

__pycache__/
*.py[cod]

eval_results.json
eval_results_full.json

# FAISS index + passage store (rebuild with: python main.py ingest)
index/
